import 'package:flutter/material.dart';

class FilterScreen extends StatelessWidget {
  static String routeNameScreen = '/filter-detail';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('FILTER !')),
    );
  }
}
